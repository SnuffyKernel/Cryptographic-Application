#pragma once

#include <iostream>
#include <fstream>
#include <vector>
#include <string>

using namespace std;

string new_text(string& Letter, string& key, const string& text, const bool& code, const bool& file);